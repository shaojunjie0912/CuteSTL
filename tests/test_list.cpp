#include <bits/stdc++.h>
#include <fmt/core.h>

using fmt::println;

int main() {
    std::list<char> std_ch_list{'a', 'b', 'c', 'd'};
    // for (auto const& ch : std_ch_list) {
    //     fmt::print("{} ", ch);
    // }
    return 0;
}